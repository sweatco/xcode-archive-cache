language: objective-c
branches:
  only:
    - master
osx_image: xcode11.5
cache:
  bundler: true
before_install:
  - bundle install --deployment && pod repo update
install: true
script:
  - bundle exec rspec && cd spec && ./integration_test.sh second
